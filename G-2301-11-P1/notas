CC = gcc
CCFLAGS = -g

gcc:
	-o para dar nombre al objeto
	-c para compilar solo a objeto.

hilos mejor que procesos

"""
El makefile debe comprimir los ficheros y los ficheros ejecutables se tienen  que borrar antes de comprimir

